spring:
  application:
    name: shares
  config:
    import: optional:configserver:http://localhost:8888
#spring:
#  application:
#    name: students
#  config:
#    import: optional:configserver:http://localhost:8888
#debug: true